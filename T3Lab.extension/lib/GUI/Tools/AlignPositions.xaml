<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Align Positions"
        Width="780" Height="600"
        MinWidth="620" MinHeight="480"
        Background="White"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="64"
                      ResizeBorderThickness="5"
                      GlassFrameThickness="0"
                      CornerRadius="8"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Section Header -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="Margin"     Value="0,14,0,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- PRIMARY button - blue, white text -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background"      Value="#3498DB"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="Padding"         Value="12,6"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDC3C7"/>
                    <Setter Property="Cursor"     Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- SECONDARY button - light gray, dark text -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background"      Value="#ECF0F1"/>
            <Setter Property="Foreground"      Value="#2C3E50"/>
            <Setter Property="Padding"         Value="12,6"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush"     Value="#BDC3C7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D5DBDB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#ECF0F1"/>
                    <Setter Property="Foreground" Value="#BDC3C7"/>
                    <Setter Property="Cursor"     Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- SUCCESS button - green -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#27AE60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E8449"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#BDC3C7"/>
                    <Setter Property="Cursor"     Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- WINDOW CONTROL button -->
        <Style x:Key="WinCtrlButton" TargetType="Button">
            <Setter Property="Width"           Value="40"/>
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ECF0F1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- CLOSE button - red on hover -->
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WinCtrlButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E74C3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══ Row 0 : Title Bar ═══ -->
        <Grid Grid.Row="0" Background="White" Height="64">
            <StackPanel Orientation="Vertical" Margin="16,0,0,0" VerticalAlignment="Center"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="T3Lab" FontSize="11" FontWeight="Bold"
                               Foreground="#3498DB" VerticalAlignment="Bottom" Margin="0,0,6,3"/>
                    <TextBlock Text="Align Positions" FontSize="18" FontWeight="Bold"
                               Foreground="#2C3E50"/>
                </StackPanel>
                <Separator Height="1" Background="#BDC3C7" Margin="0,2,0,2"/>
                <TextBlock Text="Snap element distances to nearest 5 or 10 mm"
                           FontSize="10" Foreground="#7F8C8D" FontStyle="Italic"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top"
                        WindowChrome.IsHitTestVisibleInChrome="True">
                <Button x:Name="btn_minimize" Style="{StaticResource WinCtrlButton}"
                        Click="minimize_button_clicked" ToolTip="Minimize">
                    <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="#005B82"/>
                </Button>
                <Button x:Name="btn_maximize" Style="{StaticResource WinCtrlButton}"
                        Click="maximize_button_clicked" ToolTip="Maximize">
                    <TextBlock Text="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="#005B82"/>
                </Button>
                <Button x:Name="btn_close" Style="{StaticResource CloseButton}"
                        Click="close_button_clicked" ToolTip="Close">
                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="#005B82"/>
                </Button>
            </StackPanel>

            <Border Height="1" VerticalAlignment="Bottom" Background="#BDC3C7"/>
        </Grid>

        <!-- ═══ Row 1 : Reference + Settings ═══ -->
        <StackPanel Grid.Row="1" Margin="20,4,20,0">

            <!-- Reference Element -->
            <TextBlock Text="REFERENCE ELEMENT" Style="{StaticResource SectionHeader}"/>
            <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="3" Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btn_pick_ref" Content="Pick Reference"
                            Style="{StaticResource PrimaryButton}" Click="pick_reference_clicked"
                            Margin="0,0,12,0"/>
                    <TextBlock x:Name="txt_ref_info" Grid.Column="1"
                               Text="No reference selected"
                               VerticalAlignment="Center" Foreground="#7F8C8D"
                               FontSize="12" FontFamily="Segoe UI"/>
                </Grid>
            </Border>

            <!-- Settings -->
            <TextBlock Text="SETTINGS" Style="{StaticResource SectionHeader}"/>
            <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="3" Padding="12,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                        <TextBlock Text="Units:" VerticalAlignment="Center" Width="70"
                                   Foreground="#2C3E50" FontSize="12" FontFamily="Segoe UI"/>
                        <RadioButton x:Name="rb_metric" Content=" Metric (mm)" IsChecked="True"
                                     VerticalAlignment="Center" Margin="0,0,20,0" Checked="unit_changed"/>
                        <RadioButton x:Name="rb_imperial" Content=" Imperial (inch)"
                                     VerticalAlignment="Center" Checked="unit_changed"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Grid.Row="1">
                        <TextBlock Text="Snap to:" VerticalAlignment="Center" Width="70"
                                   Foreground="#2C3E50" FontSize="12" FontFamily="Segoe UI"/>
                        <RadioButton x:Name="rb_snap1" Content=" 5 mm" IsChecked="True"
                                     VerticalAlignment="Center" Margin="0,0,20,0"
                                     FontFamily="Segoe UI" FontSize="12" Foreground="#2C3E50"
                                     Checked="snap_changed"/>
                        <RadioButton x:Name="rb_snap2" Content=" 10 mm"
                                     VerticalAlignment="Center" Margin="0,0,20,0"
                                     FontFamily="Segoe UI" FontSize="12" Foreground="#2C3E50"
                                     Checked="snap_changed"/>
                        <RadioButton x:Name="rb_snap3" Content=" 20 mm"
                                     VerticalAlignment="Center"
                                     FontFamily="Segoe UI" FontSize="12" Foreground="#2C3E50"
                                     Checked="snap_changed"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Elements header row -->
            <TextBlock Text="ELEMENTS TO ALIGN" Style="{StaticResource SectionHeader}"/>
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="btn_select" Content="Refresh Elements"
                        Style="{StaticResource SecondaryButton}" Click="select_elements_clicked"
                        Margin="0,0,12,0"/>
                <TextBlock x:Name="txt_element_count" Grid.Column="1"
                           Text="No elements collected"
                           VerticalAlignment="Center" Foreground="#7F8C8D"
                           FontSize="12" FontFamily="Segoe UI"/>
            </Grid>
        </StackPanel>

        <!-- ═══ Row 2 : DataGrid (fills remaining space) ═══ -->
        <DataGrid x:Name="dg_elements" Grid.Row="2" Margin="20,0,20,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#ECF0F1"
                  Background="White"
                  BorderBrush="#BDC3C7"
                  BorderThickness="1"
                  FontFamily="Segoe UI"
                  FontSize="12"
                  AlternatingRowBackground="#F8F9FA">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background"      Value="#ECF0F1"/>
                    <Setter Property="Foreground"      Value="#2C3E50"/>
                    <Setter Property="FontWeight"      Value="SemiBold"/>
                    <Setter Property="Padding"         Value="8,6"/>
                    <Setter Property="BorderBrush"     Value="#BDC3C7"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Height"          Value="34"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#EBF5FB"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#D6EAF8"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header=""
                    Binding="{Binding Apply}" Width="40"/>
                <DataGridTextColumn Header="Category"
                    Binding="{Binding Category}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name"
                    Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="ID"
                    Binding="{Binding ElementId}" Width="70" IsReadOnly="True"/>
                <DataGridTextColumn Header="Start Pt (X, Y)"
                    Binding="{Binding StartPt}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="End Pt (X, Y)"
                    Binding="{Binding EndPt}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Center Dist"
                    Binding="{Binding LocDist}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="Angle (°)"
                    Binding="{Binding AngleStr}" Width="70" IsReadOnly="True"/>
                <DataGridTextColumn Header="Correction"
                    Binding="{Binding Correction}" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- ═══ Row 3 : Info box + Apply ═══ -->
        <StackPanel Grid.Row="3" Margin="20,10,20,12">
            <!-- Info / Tip box -->
            <Border BorderBrush="#3498DB" BorderThickness="1" Background="#E8F4F8"
                    CornerRadius="2" Padding="10" Margin="0,0,0,10">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                        <TextBlock Text="Tip:" FontWeight="Bold" Foreground="#2980B9"
                                   Margin="0,0,5,0" FontFamily="Segoe UI" FontSize="11"/>
                        <TextBlock Foreground="#2C3E50" FontFamily="Segoe UI" FontSize="11"
                                   Text="Pick a Grid, Wall, or Column as reference."/>
                    </StackPanel>
                    <TextBlock Foreground="#2C3E50" FontFamily="Segoe UI" FontSize="11" Margin="32,0,0,0"
                               Text="Elements whose distance is already a clean multiple of 5/10 mm are hidden."/>
                </StackPanel>
            </Border>

            <Button x:Name="btn_apply" Content="Apply Corrections"
                    Style="{StaticResource SuccessButton}" Click="apply_clicked"
                    HorizontalAlignment="Right" Padding="24,8"/>
        </StackPanel>

        <!-- ═══ Row 4 : Status Bar ═══ -->
        <Border Grid.Row="4" Background="#F8F9FA" BorderBrush="#BDC3C7"
                BorderThickness="0,1,0,0" Padding="14,6">
            <TextBlock x:Name="status_text" FontSize="11" Foreground="#7F8C8D"
                       FontFamily="Segoe UI" Text="Ready"/>
        </Border>
    </Grid>
</Window>

