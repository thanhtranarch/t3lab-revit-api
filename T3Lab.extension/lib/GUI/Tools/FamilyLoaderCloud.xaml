<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid.Resources>
        <!-- Styles -->
        <Style TargetType="Button" x:Key="FolderButtonStyle">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="True"/>
        </Style>
    </Grid.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cloud Source Info Bar -->
        <Border Grid.Row="0" Background="#F8F9FA" Padding="10" BorderBrush="#BDC3C7" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Margin="10,0,0,0">
                    <TextBlock Text="☁️ Cloud Mode"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"
                               FontWeight="Bold"
                               Foreground="#3498DB"/>
                    <TextBlock x:Name="txt_web_link"
                               Text="[View Online]"
                               VerticalAlignment="Center"
                               Foreground="#3498DB"
                               TextDecorations="Underline"
                               Cursor="Hand"
                               ToolTip="Open cloud family library in browser: https://t3stu-dojk2t66r-tien-thanh-trans-projects.vercel.app"
                               Margin="5,0,0,0"/>
                </StackPanel>
                <TextBlock x:Name="txt_current_folder"
                           Text="Loading families from Vercel Cloud..."
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           FontWeight="SemiBold"
                           Foreground="#2C3E50"/>
            </DockPanel>
        </Border>

        <!-- Search Bar -->
        <Border Grid.Row="1" Padding="10" BorderBrush="#BDC3C7" BorderThickness="0,0,0,1">
            <DockPanel>
                <TextBlock Text="🔍"
                           DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Margin="5,0,10,0"/>
                <TextBox x:Name="txt_search"
                         Height="32"
                         VerticalContentAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Padding" Value="8,5"/>
                            <Setter Property="BorderBrush" Value="#BDC3C7"/>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </DockPanel>
        </Border>

        <DockPanel Grid.Row="2">
            <!-- Category Tree on Left -->
            <Border DockPanel.Dock="Left"
                    Width="250"
                    BorderBrush="#BDC3C7"
                    BorderThickness="0,0,1,0"
                    Background="#F8F9FA">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top"
                               Text="Categories"
                               FontWeight="Bold"
                               FontSize="14"
                               Padding="10"
                               Background="#ECF0F1"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TreeView x:Name="tree_categories"
                                  Padding="5"
                                  BorderThickness="0"
                                  Background="Transparent">
                        </TreeView>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- Family Grid on Right -->
            <DockPanel>
                <Border DockPanel.Dock="Top"
                        Background="#ECF0F1"
                        Padding="10"
                        BorderBrush="#BDC3C7"
                        BorderThickness="0,0,0,1">
                    <DockPanel>
                        <TextBlock x:Name="txt_result_count"
                                   Text="0 families found"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    DockPanel.Dock="Right">
                            <Button x:Name="btn_select_all"
                                    Content="Select All"
                                    Margin="5,0"
                                    Padding="8,4"/>
                            <Button x:Name="btn_select_none"
                                    Content="Select None"
                                    Padding="8,4"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto"
                              Padding="10">
                    <ItemsControl x:Name="items_families">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#BDC3C7"
                                        BorderThickness="1"
                                        Padding="8"
                                        Margin="5"
                                        Width="140"
                                        Height="180"
                                        Background="White">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="#3498DB"/>
                                                    <Setter Property="BorderThickness" Value="2"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <CheckBox x:Name="chk_family"
                                                  IsChecked="{Binding IsChecked}"
                                                  HorizontalAlignment="Left"
                                                  Margin="0,0,0,5"/>
                                        <Image Source="{Binding Thumbnail}"
                                               Height="90"
                                               Stretch="Uniform"
                                               Margin="0,5"/>
                                        <TextBlock Text="{Binding Name}"
                                                   TextAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   FontSize="11"
                                                   Height="40"
                                                   ToolTip="{Binding FullPath}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </DockPanel>

        <!-- Action Buttons at Bottom -->
        <Border Grid.Row="3"
                Background="#F8F9FA"
                Padding="15"
                BorderBrush="#BDC3C7"
                BorderThickness="0,1,0,0">
            <StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock x:Name="txt_selected_count"
                           Text="0 families selected"
                           VerticalAlignment="Center"
                           Margin="0,0,20,0"
                           FontWeight="SemiBold"/>
                <Button x:Name="btn_load"
                        Content="Load"
                        Style="{StaticResource ActionButtonStyle}"
                        Background="#3498DB"
                        Foreground="White"
                        Margin="5,0"
                        IsEnabled="False"/>
                <Button x:Name="btn_cancel"
                        Content="Cancel"
                        Style="{StaticResource ActionButtonStyle}"
                        Margin="5,0"/>
            </StackPanel>
            <!-- Copyright -->
            <TextBlock Text="© Copyright by T3Lab"
                       FontSize="10" Foreground="#7F8C8D"
                       HorizontalAlignment="Center"
                       Margin="0,6,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Grid>
